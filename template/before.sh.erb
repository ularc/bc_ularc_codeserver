# Export host, port, and security credentials
export host=$(hostname)
export port=$(find_port 8080)
export password=$(create_passwd 16)

# Generate connection.yml for Open OnDemand's proxy
cat <<EOF > connection.yml
host: "${host}"
port: "${port}"
password: "${password}"
type: codeserver
EOF
